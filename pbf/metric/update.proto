syntax = "proto3";

package metric;
option go_package = ".;metric";

// Metrics are not supported to be updated at this point in time. The apischema
// and its corresponding implementation might change that in the future.

message UpdateI {}
message UpdateO {}
